#This file will define environment variables that our application will use when running the 
#microservice during testing, and will be different from how we run it in development or production

# .env

################################################################################
# Environment variables with values for running the tests. This file can be
# committed to git, since it's only used for testing, and won't contain secrets.
################################################################################

# HTTP Port (defaults to 8080)
PORT=8080

# .htpasswd file to use in testing
HTPASSWD_FILE=tests/.htpasswd

API_URL=http://localhost:8080/

LOG_LEVEL=debug

# AWS_COGNITO_POOL_ID=?
# AWS_COGNITO_CLIENT_ID=